<Window x:Class="CurrencyConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.com/icons/fonts"
        xmlns:local="clr-namespace:CurrencyConverter"
        mc:Ignorable="d"
        Title="Currency Converter"
        Icon="app.ico"
        Height="520" Width="450"
        MinHeight="520" MinWidth="410"
        WindowStartupLocation="CenterScreen"
        Background="#FFF4F5F7"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF6B7280" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="2,0,0,6" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="38" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderBrush" Value="#FFD5D8DE" />
            <Setter Property="Background" Value="White" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="38" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="#FF2563EB" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF1D4ED8" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF1E40AF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#FF9CA3AF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundIconButton" TargetType="Button">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="White"
                                BorderBrush="#FFD5D8DE"
                                BorderThickness="1"
                                CornerRadius="17">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFEFF3FB" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24,22,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Currency Converter"
                           FontSize="22" FontWeight="SemiBold" Foreground="#FF111827" />
                <TextBlock Text="Reference rates from the European Central Bank"
                           FontSize="12" Foreground="#FF6B7280" Margin="0,4,0,0" />
            </StackPanel>
            <Button Grid.Column="1"
                    x:Name="RefreshButton"
                    Style="{StaticResource RoundIconButton}"
                    ToolTip="Reload currency list"
                    VerticalAlignment="Top"
                    Click="RefreshButton_Click">
                <fa:FontAwesome x:Name="RefreshIcon"
                                Icon="Solid_ArrowRotateRight"
                                FontSize="14"
                                Foreground="#FF374151" />
            </Button>
        </Grid>

        <!-- Amount -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="AMOUNT" Style="{StaticResource Label}" />
            <TextBox x:Name="AmountBox"
                     Text="1"
                     MaxLength="18"
                     PreviewTextInput="AmountBox_PreviewTextInput"
                     TextChanged="Input_Changed" />
        </StackPanel>

        <!-- From / swap / To -->
        <Grid Grid.Row="2" Margin="0,0,0,22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="FROM" Style="{StaticResource Label}" />
                <ComboBox x:Name="FromBox"
                          DisplayMemberPath="Display"
                          SelectedValuePath="Code"
                          SelectionChanged="Input_Changed" />
            </StackPanel>

            <Button Grid.Column="1"
                    x:Name="SwapButton"
                    Style="{StaticResource RoundIconButton}"
                    ToolTip="Swap currencies"
                    Margin="12,20,12,0"
                    VerticalAlignment="Top"
                    Click="SwapButton_Click">
                <fa:FontAwesome Icon="Solid_ArrowRightArrowLeft"
                                FontSize="14"
                                Foreground="#FF374151" />
            </Button>

            <StackPanel Grid.Column="2">
                <TextBlock Text="TO" Style="{StaticResource Label}" />
                <ComboBox x:Name="ToBox"
                          DisplayMemberPath="Display"
                          SelectedValuePath="Code"
                          SelectionChanged="Input_Changed" />
            </StackPanel>
        </Grid>

        <!-- Convert -->
        <Button Grid.Row="3"
                x:Name="ConvertButton"
                Style="{StaticResource AccentButton}"
                IsDefault="True"
                Click="ConvertButton_Click">
            <StackPanel Orientation="Horizontal">
                <fa:FontAwesome x:Name="ConvertIcon"
                                Icon="Solid_RightLeft"
                                FontSize="13"
                                Foreground="White"
                                VerticalAlignment="Center"
                                Margin="0,0,8,0" />
                <TextBlock Text="Convert" VerticalAlignment="Center" />
            </StackPanel>
        </Button>

        <!-- Result -->
        <Border Grid.Row="4"
                Margin="0,20,0,0"
                Padding="18,16"
                Background="White"
                BorderBrush="#FFE3E6EC"
                BorderThickness="1"
                CornerRadius="8">
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="ResultText"
                           Text="&#x2014;"
                           FontSize="26" FontWeight="SemiBold"
                           Foreground="#FF111827"
                           TextWrapping="Wrap" />
                <TextBlock x:Name="RateText"
                           Text="Enter an amount and pick two currencies."
                           FontSize="12" Foreground="#FF6B7280"
                           Margin="0,8,0,0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="5"
                   x:Name="StatusText"
                   Margin="2,12,0,0"
                   FontSize="12"
                   Foreground="#FF6B7280"
                   TextWrapping="Wrap"
                   Text="Loading currencies..." />
    </Grid>
</Window>
